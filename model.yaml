# model:
#   Class: BasicLSTM
#   params:
#     input_size: 12
#     hidden_size: 64
#     output_size: 5
#     num_layers: 3
#     dropout: 0.3

model:
  Class: EncoderDecoder
  description: Encoder-Decoder model with CNN, LSTM, RNN layers
  future_work: Switch to LSTM decoder
  params:
    input_size: 12
    output_size: 5
    hidden_cnn_channels: 64
    rnn_input_channels: 64
    hidden_state_size: 64
    num_layers: 3
    dropout: 0.2
    forecast_horizon: ${ forecast_horizon }